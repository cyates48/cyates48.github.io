import React, { Component } from 'react';
// import './App.css';

class Header extends Component {
    state = { };

  render() {
    return (
      <header className="Header">
        <p> First p tag lalala </p>
      </header>
    );
  }
}

export default Header;
